"""without __init__.py, Python will not recognize the directory as a package, 
and you won't be able to import modules from it. 
The __init__.py file can be empty, 
but it can also execute initialization code for the package 
 or set the __all__ variable to define the 
public interface of the package.
"""